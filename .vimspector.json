{
  "configurations": {
    "Launch currently open script": {
      "adapter": "debugpy",
      "configuration": {
        "name": "Launch currently open script",
        "type": "python",
        "request": "launch",
        "program": "${file}",
        "cwd": "${fileDirname}"
      }
    }
  }
}
